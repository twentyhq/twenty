{
  "install": "yarn install",
  "start": "(sudo service docker start || service docker start) && sleep 2 && bash packages/twenty-utils/setup-dev-env.sh && npx nx database:reset twenty-server",
  "terminals": [
    {
      "name": "Development Server",
      "command": "yarn start"
    }
  ]
}
