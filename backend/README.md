# Backend stack

- Aquí vive el código del backend: actualmente `twenty-server` (enlazado desde `packages/twenty-server`).
- Coloca futuros servicios de backend aquí para mantener separación clara frente a frontend y data.
- Los comandos de Nx siguen funcionando vía el junction en `packages/twenty-server`.
