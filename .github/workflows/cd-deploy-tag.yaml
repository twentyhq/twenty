name: CD deploy tag

permissions:
  contents: read

on:
  push:
    tags:
      - 'twenty/v*'
      - 'npm/v*'

defaults:
  run:
    shell: bash --noprofile --norc -euo pipefail {0}

jobs:
  dispatch-tag:
    timeout-minutes: 3
    runs-on: ubuntu-latest
    steps:
      - name: Resolve dispatch target from tag
        id: target
        env:
          REF_NAME: ${{ github.ref_name }}
        run: |
          case "$REF_NAME" in
            twenty/v*)
              event_type=auto-deploy-twenty
              version="${REF_NAME#twenty/v}"
              ;;
            npm/v*)
              event_type=auto-publish-npm
              version="${REF_NAME#npm/v}"
              ;;
            *)
              echo "Unsupported tag '$REF_NAME', expected 'twenty/v*' or 'npm/v*'." >&2
              exit 1
              ;;
          esac
          printf 'event_type=%s\n' "$event_type" >> "$GITHUB_OUTPUT"
          printf 'version=%s\n' "$version" >> "$GITHUB_OUTPUT"

      - name: Repository Dispatch
        env:
          GH_TOKEN: ${{ secrets.TWENTY_INFRA_TOKEN }}
          EVENT_TYPE: ${{ steps.target.outputs.event_type }}
          REF_NAME: ${{ github.ref_name }}
          VERSION: ${{ steps.target.outputs.version }}
        run: |
          gh api repos/twentyhq/twenty-infra/dispatches \
            -f "event_type=$EVENT_TYPE" \
            -f "client_payload[github][ref_name]=$REF_NAME" \
            -f "client_payload[version]=$VERSION"
