table:
  name: workspaces
  schema: public
select_permissions:
  - role: user
    permission:
      columns:
        - display_name
        - domain_name
        - logo
        - created_at
        - deleted_at
        - updated_at
        - id
      filter:
        id:
          _eq: X-Hasura-Workspace-Id
