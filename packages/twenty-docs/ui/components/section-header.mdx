---
title: SectionHeader
description: Combine a section heading, supporting description, and optional action.
---

import { StoryEmbed } from '/snippets/ui/StoryEmbed.mdx';
import SectionHeaderReference from '/snippets/ui/generated/components/section-header.mdx';

`SectionHeader` combines [Heading](/ui/primitives/typography/heading) with a description and an optional `adornment`. Descriptions are ordinary text associated with the heading, so they do not add levels to the document outline.

<StoryEmbed
  storyId="ui-components-sectionheader--documentation"
  title="Section header example"
  height={200}
/>

## Add a description and action

```tsx
import { SectionHeader } from 'twenty-ui/components';
import { Button } from 'twenty-ui/primitives/input';

export const WorkspaceSection = () => (
  <SectionHeader
    title="Workspace settings"
    description="Manage your workspace name and preferences."
    adornment={<Button title="Edit workspace" />}
  />
);
```

`level`, `size`, and `color` configure the heading. They default to `2`, `md`, and `primary`. The root is a `div` with one spacing-4 margin after it. Use native `style` or `className` to adjust that spacing.

## Description overflow

String descriptions preserve line breaks, turn URLs into links, and show up to five lines. Set `descriptionLineClamp` to a positive integer to choose another limit. Hovering or focusing an overflowing description reveals its full text; Escape dismisses the tooltip.

Pass a React node for richer supporting content. Rich descriptions render as supplied, without automatic truncation or tooltips.

## Root composition

Native attributes, refs, class names, styles, and `render` apply to the outer container. For example, `render={<header />}` changes that container while retaining its heading, description, and action.

## Props

<SectionHeaderReference />
