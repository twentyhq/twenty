---
title: Configuración de Dominios
description: Configura el dominio del espacio de trabajo, los dominios de acceso aprobados y los dominios personalizados de la app.
---

La configuración de dominios se encuentra en tres lugares, según lo que estés configurando.

## Dominio del espacio de trabajo

Configure bajo **Ajustes → General**.

Edita el nombre de tu subdominio o establece un dominio personalizado para tu espacio de trabajo.

### Personalizar dominio

1. Haz clic en **Subdominio** para editar tu subdominio (por ejemplo, `tuempresa.twenty.com`)
2. O haga clic en **dominio personalizado** para configurar un dominio personalizado (por ejemplo, `crm.yourcompany.com`)

Para dominios personalizados, tendrás que configurar los ajustes de DNS con tu proveedor de dominio.

## Dominios Aprobados

Configura en **Ajustes → Miembros → Invitar**.

Cualquiera con una dirección de correo electrónico en estos dominios puede registrarse automáticamente en este espacio de trabajo.

### Añadir Dominio de Acceso Aprobado

1. Haz clic en **Añadir Dominio de Acceso Aprobado**
2. Introduce el dominio de tu empresa (p. ej., `yourcompany.com`)
3. Guardar

Una vez configurado, cualquiera con una dirección de correo electrónico en ese dominio puede unirse a tu espacio de trabajo sin necesidad de una invitación directa.

<Note>
Esto es útil para permitir que todo tu equipo se autorregistre y, al mismo tiempo, mantener el espacio de trabajo restringido a tu organización.
</Note>

## Dominios personalizados de la app

Configura en **Ajustes → Aplicaciones → tu app → Ajustes → URL de la app**.

Las apps que exponen rutas en la web se sirven desde una URL dedicada, que se muestra en la sección **URL de la app**. En su lugar, puedes servir una app desde un dominio que poseas.

### Agregar dominio personalizado

1. Haga clic en **dominio personalizado**
2. Introduce el dominio que quieres usar (p. ej., `app.yourcompany.com`)
3. Configura los ajustes de DNS según las instrucciones
4. Verifica el dominio

Solo las rutas de esa app son accesibles en el dominio. Los certificados SSL se aprovisionan automáticamente.
