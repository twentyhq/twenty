---
title: Ikonin valitsin
image: /images/user-guide/github/github-header.png
---

<Frame>
  <img src="/images/user-guide/github/github-header.png" alt="Header" />
</Frame>

Avattavaan valikkoon perustuva ikonivalitsin, joka mahdollistaa käyttäjän valita ikonin listasta.

<Tabs>

<Tab title="Usage">

```jsx
import { RecoilRoot } from "recoil";
import React, { useState } from "react";
import { IconPicker } from "@/ui/input/components/IconPicker";

export const MyComponent = () => {

   const [selectedIcon, setSelectedIcon] = useState("");
   const handleIconChange = ({ iconKey, Icon }) => {
     console.log("Selected Icon:", iconKey);
     setSelectedIcon(iconKey);
   };

  return (
    <RecoilRoot>
      <IconPicker
        disabled={false}
        onChange={handleIconChange}
        selectedIconKey={selectedIcon}
        variant="primary"
      />
    </RecoilRoot>
  );
};
```

</Tab>
<Tab title="Props">

| Rekvisiitat     | Tyyppi     | Kuvaus                                                                                                                                      |
| --------------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------- |
| pois käytöstä   | boolean    | Poistaa ikonin valitsimen käytöstä, jos asetettu arvoon `true`                                                                              |
| onChange        | funktio    | The callback function triggered when the user selects an icon. It receives an object with `iconKey` and `Icon` properties   |
| selectedIconKey | string     | The key of the initially selected icon                                                                                                      |
| onClickOutside  | toiminto   | Callback function triggered when the user clicks outside the dropdown                                                                       |
| onClose         | toiminto   | Callback function triggered when the dropdown is closed                                                                                     |
| onOpen          | toiminto   | Callback function triggered when the dropdown is opened                                                                                     |
| variantti       | merkkijono | Klikattavan ikonin visuaalinen tyylivariantti. Vaihtoehdot sisältävät: `primary`, `secondary` ja `tertiary` |

</Tab>
</Tabs>
