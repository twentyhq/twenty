---
title: Водич за надоградњу
image: /images/user-guide/notes/notes_header.png
---

<Frame>
  <img src="/images/user-guide/notes/notes_header.png" alt="Header" />
</Frame>

## Опште смернице

**Always make sure to back up your database before starting the upgrade process** by running `docker exec -it {db_container_name_or_id} pg_dumpall -U {postgres_user} > databases_backup.sql`.

To restore backup, run `cat databases_backup.sql | docker exec -i {db_container_name_or_id} psql -U {postgres_user}`.

Ако сте користили Docker Compose, пратите ове кораке:

1. У терминалу, на хосту где је Twenty покренут, искључите Twenty: `docker compose down`

2. Надоградите верзију променом `TAG` вредности у .env фајлу поред вашег docker-compose. ( Препоручујемо коришћење `major.minor` верзије као што је `v0.53` )

3. Вратите Twenty поново онлајн са `docker compose up -d`

Ако желите да надоградите вашу инстанцу за неколико верзија, на пример са v0.33.0 на v0.35.0, морате да надограђујете вашу инстанцу секвенцијално, у овом примеру од v0.33.0 до v0.34.0, затим од v0.34.0 до v0.35.0.

**Уверите се да након сваке надограђене верзије имате непокварену резервну копију.**

## Кораци за надоградњу специфични за верзију

## v1.0

Здраво Twenty v1.0! 🎉

## v0.60

### Побољшања перформанси

Све интеракције са metadata API-јем су оптимизоване за боље перформансе, посебно за манипулацију метаподацима објеката и операције креирања радног простора.

Рефакторисали смо нашу стратегију кеширања како бисмо приоритетно користили ударе кеша уместо упита за базу података када је то могуће, значајно побољшавајући перформансе metadata API операција.

Ако наиђете на било какве проблеме са извођењем након надоградње, можда ћете морати испразнити свој кеш како бисте га синхронизовали са најновијим изменама. Покрените ову команду у вашем twenty-server контејнеру:

```bash
yarn command:prod cache:flush
```

### v0.55

Надоградите вашу Twenty инстанцу да користи v0.55 слику

Не морате више покретати ниједну команду, нова слика ће аутоматски преузети све потребне миграције.

### Грешка `Корисник нема дозволу`

Ако наиђете на грешке са ауторизацијом на већини захтева након надоградње, можда ћете морати испразнити свој кеш да бисте поново израчунали најновије дозволе.

У вашем `twenty-server` контејнеру, покрените:

```bash
yarn command:prod cache:flush
```

Овај проблем је специфичан за ову верзију Twenty и не би требао бити потребан за будуће надоградње.

### v0.54

Од верзије `0.53`, нису потребне никакве ручне акције.

#### Депрекција шеме метаподатака

Сјединили смо `metadata` шему у `core` како бисмо поједноставили преузимање података из `TypeORM`.
Сјединили смо корак `migrate` команде у оквиру `upgrade` команде. Не препоручујемо ручно покретање `migrate` у било којем од ваших сервер/радник контејнера.

### Од v0.53

Почевши од `0.53`, надоградња се програмски извршава у оквиру `DockerFile`, што значи да од сада више не требате ручно покретати ниједну команду.

Уверите се да настављате да надограђујете вашу инстанцу секвенцијално, без прескакања било које главне верзије (нпр. `0.43.3` до `0.44.0` је дозвољено, али `0.43.1` до `0.45.0` није), у супротном би могло доћи до десинхронизације верзије радне површине што би довело до грешке у извођењу и недостајуће функционалности.

Да бисте проверили да ли је радни простор исправно мигриран, можете прегледати његову верзију у бази података у `core.workspace` табели.

Увек би требало да буде у опсегу ваше текуће Twenty инстанце `major.minor` верзије, можете видети вашу верзију инстанце у административном панелу (на `/settings/admin-panel`, доступно ако је вашем кориснику постављено `canAccessFullAdminPanel` својство на true у бази података) или покретањем `echo $APP_VERSION` у вашем `twenty-server` контејнеру.

Да бисте исправили десинхронизовану верзију радног простора, мораћете да надоградите са одговарајуће Twenty верзије пратећи повезани водич за надоградњу секвенцијално и тако даље све док не достигне жељену верзију.

#### уклањање `auditLog`

Избрисали смо стандардни објекат auditLog, што значи да би се величина ваше резервне копије могла значајно смањити након ове миграције.

### v0.51 до v0.52

Надоградите вашу Twenty инстанцу да користи v0.52 слику

```
yarn database:migrate:prod
yarn command:prod upgrade
```

#### Имам радни простор блокиран у верзији између `0.52.0` и `0.52.6`

Нажалост, `0.52.0` и `0.52.6` су потпуно уклоњени са dockerHub-а.
Мораћете ручно да ажурирате верзију вашег радног простора на `0.51.0` у бази података и надоградите користећи twenty верзију `0.52.11` пратећи горњи водич за надоградњу.

### v0.50 до v0.51

Надоградите вашу Twenty инстанцу да користи v0.51 слику

```
yarn database:migrate:prod
yarn command:prod upgrade
```

### v0.44.0 до v0.50.0

Надоградите вашу Twenty инстанцу да користи v0.50.0 слику

```
yarn database:migrate:prod
yarn command:prod upgrade
```

#### Мутација Docker-compose.yml

Ова верзија укључује мутацију `docker-compose.yml` како би се `worker` сервису обезбедио приступ `server-local-data` волумену.
Ажурирајте ваш локални `docker-compose.yml` са [v0.50.0 docker-compose.yml](https://github.com/twentyhq/twenty/blob/v0.50.0/packages/twenty-docker/docker-compose.yml)

### v0.43.0 до v0.44.0

Надоградите вашу Twenty инстанцу да користи v0.44.0 слику

```
yarn database:migrate:prod
yarn command:prod upgrade
```

### v0.42.0 до v0.43.0

Надоградите вашу Twenty инстанцу да користи v0.43.0 слику

```
yarn database:migrate:prod
yarn command:prod upgrade
```

У овој верзији такође смо прешли на postgres:16 слику у docker-compose.yml.

#### (Опција 1) Миграција базе података

Одржавање постојеће postgres-spilo слике је у реду, али ћете морати замрзнути верзију у вашем docker-compose.yml на 0.43.0.

#### (Опција 2) Миграција базе података

Ако желите да мигрирате вашу базу података на нову postgres:16 слику, следите ове кораке:

1. Испразните вашу базу података из старог postgres-spilo контејнера

```
docker exec -it twenty-db-1 sh
pg_dump -U {YOUR_POSTGRES_USER} -d {YOUR_POSTGRES_DB} > databases_backup.sql
exit
docker cp twenty-db-1:/home/postgres/databases_backup.sql .
```

Уверите се да ваш фајл са меморијом није празан.

2. Надоградите ваш docker-compose.yml да користи postgres:16 слику као у [docker-compose.yml](https://raw.githubusercontent.com/twentyhq/twenty/main/packages/twenty-docker/docker-compose.yml) фајлу.

3. Вратите базу података на нови postgres:16 контејнер

```
docker cp databases_backup.sql twenty-db-1:/databases_backup.sql
docker exec -it twenty-db-1 sh
psql -U {YOUR_POSTGRES_USER} -d {YOUR_POSTGRES_DB} -f databases_backup.sql
exit
```

### v0.41.0 до v0.42.0

Надоградите вашу Twenty инстанцу да користи v0.42.0 слику

```
yarn database:migrate:prod
yarn command:prod upgrade-0.42
```

**Променљиве окружења**

- Уклоњено: `FRONT_PORT`, `FRONT_PROTOCOL`, `FRONT_DOMAIN`, `PORT`
- Додато: `FRONTEND_URL`, `NODE_PORT`, `MAX_NUMBER_OF_WORKSPACES_DELETED_PER_EXECUTION`, `MESSAGING_PROVIDER_MICROSOFT_ENABLED`, `CALENDAR_PROVIDER_MICROSOFT_ENABLED`, `IS_MICROSOFT_SYNC_ENABLED`

### v0.40.0 до v0.41.0

Надоградите вашу Twenty инстанцу да користи v0.41.0 слику

```
yarn database:migrate:prod
yarn command:prod upgrade-0.41
```

**Променљиве окружења**

- Уклоњено: `AUTH_MICROSOFT_TENANT_ID`

### v0.35.0 до v0.40.0

Надоградите вашу Twenty инстанцу да користи v0.40.0 слику

```
yarn database:migrate:prod
yarn command:prod upgrade-0.40
```

**Променљиве окружења**

- Додато: `IS_EMAIL_VERIFICATION_REQUIRED`, `EMAIL_VERIFICATION_TOKEN_EXPIRES_IN`, `WORKFLOW_EXEC_THROTTLE_LIMIT`, `WORKFLOW_EXEC_THROTTLE_TTL`

### v0.34.0 до v0.35.0

Надоградите вашу Twenty инстанцу да користи v0.35.0 слику

```
yarn database:migrate:prod
yarn command:prod upgrade-0.35
```

Команда `yarn database:migrate:prod` ће применити миграције на структуру базе података (core и metadata шеме)
`yarn command:prod upgrade-0.35` преузима бригу о миграцији података свих радних простора.

**Променљиве окружења**

- Заменили смо `ENABLE_DB_MIGRATIONS` са `DISABLE_DB_MIGRATIONS` (подразумевана вредност је сада `false`, вероватно нећете морати ништа да подесите)

### v0.33.0 до v0.34.0

Надоградите вашу Twenty инстанцу да користи v0.34.0 слику

```
yarn database:migrate:prod
yarn command:prod upgrade-0.34
```

Команда `yarn database:migrate:prod` ће применити миграције на структуру базе података (core и metadata шеме)
`yarn command:prod upgrade-0.34` преузима бригу о миграцији података свих радних простора.

**Променљиве окружења**

- Уклоњено: `FRONT_BASE_URL`
- Додато: `FRONT_DOMAIN`, `FRONT_PROTOCOL`, `FRONT_PORT`

Ажурирали смо начин на који рукујемо са адресом фронтенда.
Сада можете подесити адресу фронтенда коришћењем променљивих `FRONT_DOMAIN`, `FRONT_PROTOCOL` и `FRONT_PORT`.
Ако FRONT_DOMAIN није постављен, адреса фронтенда ће се вратити на `SERVER_URL`.

### v0.32.0 до v0.33.0

Надоградите вашу Twenty инстанцу да користи v0.33.0 слику

```
yarn command:prod cache:flush
yarn database:migrate:prod
yarn command:prod upgrade-0.33
```

Команда `yarn command:prod cache:flush` ће очистити Redis кеш.
Команда `yarn database:migrate:prod` ће применити миграције на структуру базе података (core и metadata шеме)
`yarn command:prod upgrade-0.33` преузима бригу о миграцији података свих радних простора.

Starting from this version, twenty-postgres image for DB became deprecated and twenty-postgres-spilo is used instead.
If you want to keep using twenty-postgres image, simply replace `twentycrm/twenty-postgres:${TAG}` with `twentycrm/twenty-postgres` in docker-compose.yml.

### v0.31.0 до v0.32.0

Надоградите вашу Twenty инстанцу да користи v0.32.0 слику

**Миграција шеме и података**

```
yarn database:migrate:prod
yarn command:prod upgrade-0.32
```

Команда `yarn database:migrate:prod` ће применити миграције на структуру базе података (core и metadata шеме)
`yarn command:prod upgrade-0.32` преузима бригу о миграцији података свих радних простора.

**Променљиве окружења**

Ажурирали смо начин на који рукујемо са Redis конекцијом.

- Уклоњено: `REDIS_HOST`, `REDIS_PORT`, `REDIS_USERNAME`, `REDIS_PASSWORD`
- Додато: `REDIS_URL`

Ажурирајте ваш `.env` фајл да користи нову `REDIS_URL` променљиву уместо индивидуалних Redis параметара конекције.

Такође смо поједноставили начин на који рукујемо са JWT токенима.

- Уклоњено: `ACCESS_TOKEN_SECRET`, `LOGIN_TOKEN_SECRET`, `REFRESH_TOKEN_SECRET`, `FILE_TOKEN_SECRET`
- Додато: `APP_SECRET`

Ажурирајте ваш `.env` фајл да користи нову `APP_SECRET` променљиву уместо индивидуалних тајни токена (можете користити исту тајну као пре или генерисати нови случајан низ)

**Повезани налог**

Ако користите повезани налог за синхронизацију ваших Google имејлова и календара, мораћете да активирате [People API](https://developers.google.com/people) на вашем Google Admin конзоли.

### v0.30.0 до v0.31.0

Надоградите вашу Twenty инстанцу да користи v0.31.0 слику

**Миграција шеме и података**:

```
yarn database:migrate:prod
yarn command:prod upgrade-0.31
```

Команда `yarn database:migrate:prod` ће применити миграције на структуру базе података (core и metadata шеме)
`yarn command:prod upgrade-0.31` преузима бригу о миграцији података свих радних простора.

### v0.24.0 до v0.30.0

Надоградите вашу Twenty инстанцу да користи v0.30.0 слику

**Breaking change**:
To enhance performances, Twenty now requires redis cache to be configured. Ажурирали смо наш [docker-compose.yml](https://raw.githubusercontent.com/twentyhq/twenty/main/packages/twenty-docker/docker-compose.yml) да ово одрази.
Уверите се да сте ажурирали вашу конфигурацију и ажурирали променљиве окружења у складу са тим:

```
REDIS_HOST={your-redis-host}
REDIS_PORT={your-redis-port}
CACHE_STORAGE_TYPE=redis
```

**Миграција шеме и података**:

```
yarn database:migrate:prod
yarn command:prod upgrade-0.30
```

Команда `yarn database:migrate:prod` ће применити миграције на структуру базе података (core и metadata шеме)
`yarn command:prod upgrade-0.30` преузима бригу о миграцији података свих радних простора.

### v0.23.0 до v0.24.0

Надоградите вашу Twenty инстанцу да користи v0.24.0 слику

Покрените следеће команде:

```
yarn database:migrate:prod
yarn command:prod upgrade-0.24
```

Команда `yarn database:migrate:prod` ће применити миграције на структуру базе података (core и metadata шеме)
`yarn command:prod upgrade-0.24` преузима бригу о миграцији података свих радних простора.

### v0.22.0 до v0.23.0

Надоградите вашу Twenty инстанцу да користи v0.23.0 слику

Покрените следеће команде:

```
yarn database:migrate:prod
yarn command:prod upgrade-0.23
```

Команда `yarn database:migrate:prod` ће применити миграције на базу података.
`yarn command:prod upgrade-0.23` брине о миграцији података, укључујући пренос активности на задатке/напомене.

### v0.21.0 до v0.22.0

Надоградите вашу Twenty инстанцу да користи v0.22.0 слику

Покрените следеће команде:

```
yarn database:migrate:prod
yarn command:prod workspace:sync-metadata -f
yarn command:prod upgrade-0.22
```

Команда `yarn database:migrate:prod` ће применити миграције на базу података.
Команда `yarn command:prod workspace:sync-metadata -f` ће синхронизовати дефиницију стандардних објеката са табелама метаподатака и применити потребне миграције на постојеће радне просторе.
Команда `yarn command:prod upgrade-0.22` ће применити специфичне трансформације података да би се прилагодили новим опцијама подразумеваних захтева инструментације објекта.




