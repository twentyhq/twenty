import { ChartIcon } from '/snippets/chart-icon.mdx';

export const ChartSettingInlineIcon = ({ icon }) => (
  <span style={{ display: 'inline-flex', verticalAlign: 'middle' }}>
    <ChartIcon icon={icon} size={16} />
  </span>
);
