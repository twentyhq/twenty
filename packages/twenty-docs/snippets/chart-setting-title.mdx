import { ChartIcon } from '/snippets/chart-icon.mdx';

export const ChartSettingTitle = ({ icon, children }) => (
  <span style={{ display: 'flex', alignItems: 'center', gap: '8px' }}>
    <ChartIcon icon={icon} />
    {children}
  </span>
);
