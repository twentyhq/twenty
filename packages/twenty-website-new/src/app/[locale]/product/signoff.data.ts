import { msg } from '@lingui/core/macro';

export const SIGNOFF_COPY = {
  body: msg`Join the teams that chose to own their CRM. Start building with Twenty today.`,
  heading: {
    descriptor: msg`Ready to grow`,
    qualifier: msg`with `,
    subject: msg`Twenty?`,
  },
};
