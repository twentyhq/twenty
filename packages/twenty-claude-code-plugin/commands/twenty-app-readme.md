---
description: Prepare a Twenty app README, marketplace metadata, logo, and screenshots
---

Use the `app-readme-and-visuals` skill to create or improve a Twenty app README, npm/marketplace description, `defineApplication()` marketplace metadata, logo, screenshots, and public visual assets.

User input: $ARGUMENTS

Inspect the current app structure (`package.json`, `src/application-config.ts`, `src/`, `public/`) before writing copy. Confirm with the user before adding placeholder URLs, support addresses, or screenshots that do not yet exist.
