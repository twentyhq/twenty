---
description: Scaffold a new Twenty app with create-twenty-app and walk through setup
---

Use the `create-an-app` skill to help the user scaffold, run, extend, test, deploy, or troubleshoot a Twenty app with `create-twenty-app` and the Twenty Apps developer workflow.

User input: $ARGUMENTS

If `$ARGUMENTS` names a target directory or example, use it. Otherwise, ask the user for the app name and whether they want a minimal template or an example such as `postcard` or `hello-world`.
