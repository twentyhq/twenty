interface:
  display_name: "Retrieve Workspace Data"
  short_description: "Retrieve Twenty workspace data clearly."
  default_prompt: "Use $retrieve-and-present-data to retrieve Twenty workspace data with readable dates, labels, grouped fields, original record links, and safe visual identifiers when available."
