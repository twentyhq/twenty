# IdeaFlow Documents

This workspace package is reserved for document-related IdeaFlow capabilities built around the Twenty platform.

Stage 0 limits the package to a scaffold so future document flows can evolve in a dedicated layer instead of modifying upstream business logic.
