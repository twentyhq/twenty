// Billing settings page in the Twenty app (cloud-only). Declared once so every
// billing-related email CTA points at the same place instead of hardcoding the URL.
export const BILLING_SETTINGS_URL = 'https://app.twenty.com/settings/billing';
