import { type RecallAsyncTranscriptProvider } from 'src/logic-functions/types/RecallAsyncTranscriptProvider';

export const DEFAULT_COMPANION_TRANSCRIPT_PROVIDER: RecallAsyncTranscriptProvider =
  'recallai_async';
