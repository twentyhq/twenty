// Twenty stores imported recording artifacts, so Recall.ai media is temporary. Keep the default below Recall.ai's 168-hour free storage window.
export const DEFAULT_COMPANION_RECORDING_RETENTION_HOURS = 166;
