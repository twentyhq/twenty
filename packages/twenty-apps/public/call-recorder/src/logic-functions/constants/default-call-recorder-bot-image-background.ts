// Most logos are designed to sit on white, so it is the safest default backdrop
// for the bot's on-camera tile. Overridable per workspace with a hex color.
export const DEFAULT_CALL_RECORDER_BOT_IMAGE_BACKGROUND = '#ffffff';
