// Privacy-first default: the bot only joins meetings explicitly set to On
// until a workspace admin opts into recording every eligible meeting.
export const DEFAULT_CALL_RECORDER_AUTO_RECORD_ENABLED = false;
