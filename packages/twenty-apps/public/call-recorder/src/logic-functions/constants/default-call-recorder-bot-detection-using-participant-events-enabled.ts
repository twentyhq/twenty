// Behavioral bot detection is opt-in: it can eject a silent human, so instances
// must explicitly enable it. Name-based detection stays on unconditionally.
export const DEFAULT_CALL_RECORDER_BOT_DETECTION_USING_PARTICIPANT_EVENTS_ENABLED = false;
