# Setup

Implementation in progress. Create a workspace API key in Granola Settings → Connectors → Workspace API keys. Install the app and save the key in its settings. Registration, history import, and folder selection follow in the stack.
