// The connection lives in the app's General tab, so the banner button sends the
// admin there instead of whichever tab the banner is read from.
export const SLACK_CONNECTION_BANNER_ACTION = {
  label: 'Manage connection',
  location: '#general',
};
