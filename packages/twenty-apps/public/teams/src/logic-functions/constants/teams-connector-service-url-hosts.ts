// anyone can register a profile under trafficmanager.net, so the Bot Connector
// host there is allowed by exact match rather than by suffix
export const TEAMS_CONNECTOR_SERVICE_URL_HOSTS = ['smba.trafficmanager.net'];
