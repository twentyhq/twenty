import { RECALL_RECORDING_BOT_PREFERENCES } from 'src/logic-functions/types/recall-recording-bot-preferences';

export type RecallRecordingBotPreference =
  (typeof RECALL_RECORDING_BOT_PREFERENCES)[number];
