// FILTERED hides suspected automation and privacy proxies; ALL shows every
// observation. Both are always available to the marketer.
export enum CampaignEngagementActivityFilter {
  FILTERED = 'FILTERED',
  ALL = 'ALL',
}
