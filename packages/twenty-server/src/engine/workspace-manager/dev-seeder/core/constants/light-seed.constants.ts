// Objects excluded from seeding in light mode (demo/custom objects)
export const LIGHT_EXCLUDED_OBJECTS = [
  'pet',
  'surveyResult',
  'employmentHistory',
  'petCareAgreement',
];

// Maximum number of records per object in light mode
export const LIGHT_MAX_RECORDS = 5;
