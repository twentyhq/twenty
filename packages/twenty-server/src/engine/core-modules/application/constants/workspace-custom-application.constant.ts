// The per-workspace Custom application that owns every custom object/field.
// Its name is not translated (it is an internal, workspace-scoped app).
export const WORKSPACE_CUSTOM_APPLICATION_NAME = 'Custom';
