export enum EnvironmentVariablesGroup {
  Authentication = 'authentication',
  Email = 'email',
  ServerConfig = 'server-config',
  Logging = 'logging',
  Workspace = 'workspace',
  Other = 'other',
}
