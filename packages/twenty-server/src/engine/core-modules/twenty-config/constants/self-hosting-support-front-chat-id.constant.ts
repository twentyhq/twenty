// Front chat used by self-hosted Enterprise instances to reach Twenty support.
// It points to a dedicated Front inbox, separate from the cloud support chat,
// so self-hosted conversations land in their own queue.
export const SELF_HOSTING_SUPPORT_FRONT_CHAT_ID = '';
