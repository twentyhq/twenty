// __Host- enforces Secure, Path=/ and no Domain attribute at the browser
// level, so the cookie can never be widened to sibling subdomains.
export const USER_SESSION_SECURE_COOKIE_NAME = '__Host-twenty-session';
