# IdeaFlow AI

This workspace package is reserved for AI-facing IdeaFlow extensions that integrate with Twenty without forking its core workflows.

Stage 0 keeps the package isolated so future AI features can be introduced through a controlled customization layer.
