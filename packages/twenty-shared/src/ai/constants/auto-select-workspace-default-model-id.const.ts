// Stored on an agent that follows the workspace's agent tier instead of naming
// one of its own, so changing the workspace setting moves every such agent.
export const AUTO_SELECT_WORKSPACE_DEFAULT_MODEL_ID =
  'workspace-default-model' as const;
