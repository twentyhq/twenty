# Twenty CRM - Coolify Environment Configuration
# Copy these values into Coolify's Environment Variables section

# REQUIRED: Your domain or server URL
# Examples:
#   - https://crm.yourdomain.com (with domain and SSL)
#   - http://your-vps-ip:3000 (without domain, for testing)
SERVER_URL=https://crm.yourdomain.com

# REQUIRED: Generate with: openssl rand -base64 32
# Keep this secret and never share it!
APP_SECRET=REPLACE_WITH_GENERATED_SECRET

# REQUIRED: Strong password without special characters
PG_DATABASE_PASSWORD=REPLACE_WITH_STRONG_PASSWORD

# Database configuration (usually defaults are fine)
PG_DATABASE_USER=postgres
PG_DATABASE_NAME=default
PG_DATABASE_HOST=db
PG_DATABASE_PORT=5432

# Redis configuration (usually defaults are fine)
REDIS_URL=redis://redis:6379

# Storage configuration
STORAGE_TYPE=local

# Optional: S3 Storage (uncomment and configure if needed)
# STORAGE_TYPE=s3
# STORAGE_S3_REGION=eu-west-3
# STORAGE_S3_NAME=my-bucket-name
# STORAGE_S3_ENDPOINT=

# Optional: Email configuration (uncomment and configure if needed)
# EMAIL_FROM_ADDRESS=contact@yourdomain.com
# EMAIL_FROM_NAME="Your Company Name"
# EMAIL_SYSTEM_ADDRESS=system@yourdomain.com
# EMAIL_DRIVER=smtp
# EMAIL_SMTP_HOST=smtp.gmail.com
# EMAIL_SMTP_PORT=465
# EMAIL_SMTP_USER=your-email@gmail.com
# EMAIL_SMTP_PASSWORD=your-app-password

# Optional: Google OAuth (uncomment and configure if needed)
# MESSAGING_PROVIDER_GMAIL_ENABLED=true
# CALENDAR_PROVIDER_GOOGLE_ENABLED=true
# AUTH_GOOGLE_CLIENT_ID=your-client-id
# AUTH_GOOGLE_CLIENT_SECRET=your-client-secret
# AUTH_GOOGLE_CALLBACK_URL=https://crm.yourdomain.com/auth/google/callback
# AUTH_GOOGLE_APIS_CALLBACK_URL=https://crm.yourdomain.com/auth/google-apis/callback

# Optional: Microsoft OAuth (uncomment and configure if needed)
# CALENDAR_PROVIDER_MICROSOFT_ENABLED=true
# MESSAGING_PROVIDER_MICROSOFT_ENABLED=true
# AUTH_MICROSOFT_ENABLED=true
# AUTH_MICROSOFT_CLIENT_ID=your-client-id
# AUTH_MICROSOFT_CLIENT_SECRET=your-client-secret
# AUTH_MICROSOFT_CALLBACK_URL=https://crm.yourdomain.com/auth/microsoft/callback
# AUTH_MICROSOFT_APIS_CALLBACK_URL=https://crm.yourdomain.com/auth/microsoft-apis/callback
