export type SettingsApplicationTimelineActivityType = {
  action: string | null;
  icon?: string | null;
  id: string;
  isActive: boolean;
  isInstalled: boolean;
  label: string;
  name: string;
};
