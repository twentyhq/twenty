export enum RecordPageLayoutSingleRecordCommandKeys {
  EDIT_RECORD_PAGE_LAYOUT = 'edit-record-page-layout-single-record',
  // TODO: Save/Cancel removed from hardcoded config — LayoutCustomizationBar
  // handles them now. These still exist as server-driven commands via
  // EngineComponentKey. Decide whether to keep them server-side or remove entirely.
}
