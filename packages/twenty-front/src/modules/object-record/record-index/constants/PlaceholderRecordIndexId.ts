// Fallback instance id for hooks that require a record index id even when no
// record index is on screen (e.g. commands run from a side panel on a page
// without a view). Callers must guard at execution time.
export const PLACEHOLDER_RECORD_INDEX_ID = 'placeholder-record-index-id';
