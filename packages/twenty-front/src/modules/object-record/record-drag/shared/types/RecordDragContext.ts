/**
 * Defines the context in which record drag operations are performed.
 * Different contexts have different state management and interaction patterns.
 */
export type RecordDragContext = 'board' | 'table';
