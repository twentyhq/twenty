import type { SortableTargetDestination } from '@/navigation/utils/workspaceDndKitSortableTargetDestination';

/** Shared result shape for both sortable and string-id targets. Used by drag-over and drag-end. */
export type ResolvedDropTarget = SortableTargetDestination;
