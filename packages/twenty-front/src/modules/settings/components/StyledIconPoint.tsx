import styled from '@emotion/styled';
import { IconPoint as IconPointUI } from 'twenty-ui';

const StyledIconPoint = styled(IconPointUI)`
  margin-right: 0;
`;
export const IconPoint = StyledIconPoint;
