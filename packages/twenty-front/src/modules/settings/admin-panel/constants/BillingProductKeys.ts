// The admin panel reads productKey off the subscription item DTO as a plain
// string, so the values it compares against live here rather than being
// redeclared per consumer.
export const BASE_PRODUCT_KEY = 'BASE_PRODUCT';
export const RESOURCE_CREDIT_KEY = 'RESOURCE_CREDIT';
