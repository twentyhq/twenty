// Kept under the welcome animation hold so waiting for enrichment stays invisible, while
// still capping the wait if the enrichment request hangs.
export const WORKSPACE_SETUP_CHAT_ENRICHMENT_MAX_WAIT_MS = 2500;
