// 3 visible activation steps of 28px each (see OnboardingActivationSteps).
// Shared so OnboardingPageLoader reserves the same space and the pulsing logo
// keeps its position when the verify screen replaces the loader on cold boot.
export const ONBOARDING_ACTIVATION_STEPS_HEIGHT_IN_PX = 28 * 3;
