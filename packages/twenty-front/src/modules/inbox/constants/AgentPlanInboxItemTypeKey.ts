// The one built-in type whose body is a list of proposed tool calls rather
// than a thread or a record, so the item page draws it differently.
export const AGENT_PLAN_INBOX_ITEM_TYPE_KEY = 'agent_plan';
