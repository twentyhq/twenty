// Module widgets (Tasks, Notes...) stacked alongside others get a capped height
// so each keeps its own scroll region instead of the tab growing unbounded.
export const STACKED_MODULE_WIDGET_MAX_HEIGHT = 640;
