// Each prerendered tab is a mounted (hidden) widget tree with live queries;
// the cap bounds the memory and background work a hover trail can accumulate.
export const MAX_PRERENDERED_PAGE_LAYOUT_TABS = 3;
