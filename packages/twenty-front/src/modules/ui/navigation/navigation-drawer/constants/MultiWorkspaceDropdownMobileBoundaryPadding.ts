// On mobile the page inset is 20px and the panel overlays its trigger 5px to
// the left, so its edge lands at 16px. The default 32px boundary would make
// floating-ui flip the alignment and send the panel to the far edge instead.
export const MULTI_WORKSPACE_DROPDOWN_MOBILE_BOUNDARY_PADDING = 16;
