export enum DatabaseTriggerDefaultLabel {
  RECORD_IS_CREATED = 'Record is created',
  RECORD_IS_UPDATED = 'Record is updated',
  RECORD_IS_DELETED = 'Record is deleted',
  RECORD_IS_CREATED_OR_UPDATED = 'RECORD_IS_UPSERTED',
}
