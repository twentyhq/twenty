import { NODE_BORDER_WIDTH } from '@/workflow/workflow-diagram/constants/NodeBorderWidth';

export const CREATE_STEP_NODE_WIDTH = 24 + 8 + NODE_BORDER_WIDTH; // New step icon width + step padding + border width
