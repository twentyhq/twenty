REACT_APP_SERVER_BASE_URL=http://localhost:3000
VITE_BUILD_SOURCEMAP=false


# ———————— Optional ————————
# REACT_APP_PORT=3001
# Propel fork: URL of the softphone app hosted in the persistent dialer dock.
# Absent = dock disabled (zero behavior change).
# REACT_APP_DIALER_DOCK_URL=http://localhost:4173
# Propel fork: enable the graduated Marketing Home hub nav entry (/marketing).
# Absent = nav item hidden (the route still registers but is unreachable from nav).
# REACT_APP_PROPEL_MARKETING_HUB=true
# CHROMATIC_PROJECT_TOKEN=
# VITE_ENABLE_SSL=false
# VITE_HOST=localhost.com
# SSL_KEY_PATH="./certs/your-cert.key"
# SSL_CERT_PATH="./certs/your-cert.crt"
# IS_DEBUG_MODE=false