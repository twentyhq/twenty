import { token } from './token';

export const ICON_TOKENS = {
  size: {
    sm: token('14', { unit: 'number' }),
    md: token('16', { unit: 'number' }),
    lg: token('20', { unit: 'number' }),
    xl: token('24', { unit: 'number' }),
  },
  stroke: {
    sm: token('1.6', { unit: 'number' }),
    md: token('2', { unit: 'number' }),
    lg: token('2.5', { unit: 'number' }),
  },
};
