// The prompt egg (terminal subtree) launches the traffic-light escape
// (stage subtree); the window event is the old site's bridge between
// them, ported as-is.
export const TRAFFIC_LIGHTS_ESCAPE_EVENT = 'twenty-traffic-lights-escape';
