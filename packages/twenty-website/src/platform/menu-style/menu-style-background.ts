// The Menu's background override channel: a page writes this custom
// property (per scroll frame, imperatively); the Menu's CSS reads it with
// its scheme surface as the fallback.
export const MENU_STYLE_BACKGROUND_VAR = '--menu-style-background';
