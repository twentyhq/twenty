// The lead page's directory zone is reached by anchor from the hero CTA
// (sections) and from a partner profile's back link (partners-marketplace).
// Those two layers may not import each other, so the id lives here.
export const PARTNER_DIRECTORY_ANCHOR_ID = 'partner-directory';
