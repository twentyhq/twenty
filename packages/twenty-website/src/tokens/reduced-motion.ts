// The single source of the reduced-motion at-rule (five components carry
// bespoke disable blocks whose bodies legitimately differ).
export const REDUCED_MOTION: string = '@media (prefers-reduced-motion: reduce)';
