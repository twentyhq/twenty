import { type RhythmStep } from './rhythm';

// Horizontal page gutter, in spacing multipliers. Owned by Container so
// sections never declare their own horizontal padding.
export const GUTTER: RhythmStep = { base: 4, md: 10 };
