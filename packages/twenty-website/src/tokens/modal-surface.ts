// The contact modal's authored dark surface (ported from the old site's
// design-system Modal): a near-black panel over the dimmed, blurred page.
export const MODAL_SURFACE: { backdrop: string; panel: string } = {
  backdrop: 'rgba(28, 28, 28, 0.8)',
  panel: '#0c0c0c',
};
