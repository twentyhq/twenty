// Authored section glows (ported stops — design values, not scheme-derived).
export const GRADIENT: { heroGlow: string } = {
  heroGlow:
    'radial-gradient(ellipse 80% 60% at 50% 40%, rgba(245, 243, 240, 0.6) 0%, transparent 70%)',
};
