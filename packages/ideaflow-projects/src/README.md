# IdeaFlow Projects

This workspace package is reserved for IdeaFlow project-management extensions layered on top of Twenty.

Stage 0 keeps the package as a clean scaffold so future project features can be developed without rewriting Twenty core modules.
